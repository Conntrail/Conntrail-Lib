You are generating contrast inputs for routing analysis.

Given the following agent input, produce exactly 3 variants as JSON.

Original input: {input}

Rules:
- similar: same intent and meaning, different words. Do NOT change urgency, sentiment or emphasis.
- neutral: remove all urgency, sentiment and emphasis. Keep only the core factual request.
- opposite: invert the key dimension (urgency → routine, escalate → deprioritise, etc.)

All variants must be plausible real user inputs. Do not add explanations.
Each value must be a plain string — no nested objects, no extra keys.

Respond ONLY with valid JSON, no other text:
{{
  "similar": "write the similar variant here as a plain string",
  "neutral": "write the neutral variant here as a plain string",
  "opposite": "write the opposite variant here as a plain string"
}}
